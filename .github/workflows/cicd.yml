name: cicd selfhotes runner

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: [self-hosted,linux]

    steps:
    - name: Build the Docker image
      run: echo hello
